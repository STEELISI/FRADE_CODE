./passes/web-incoming.js:40:   * Sets timeout in request socket if it was specified in options.
./passes/web-incoming.js:49:  timeout: function timeout(req, res, options) {
./passes/web-incoming.js:50:    if(options.timeout) {
./passes/web-incoming.js:51:      req.socket.setTimeout(options.timeout);
./passes/web-incoming.js:128:    // allow outgoing socket to timeout so that we could
./common.js:112: * the timeout to 0.
